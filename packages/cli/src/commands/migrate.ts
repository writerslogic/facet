// `countless migrate`: apply D1 migrations by shelling out to wrangler. Real logic lands in T029.

export async function runMigrate(_args: string[]): Promise<number> {
	process.stdout.write('countless migrate: not yet implemented\n');
	return 0;
}
