# Audit-file loop progress

Model: one subagent per file (audit-only) → parent verifies each finding against source → fixes → gates → commits per package. 152 files total.

## Status by package
- [x] trust (21) — DONE. 3 fix batches: dbcadd3, 213c9a5, d4ebc2d. 550 tests green.
      Fixed: receipt fail-closed, canonicalize depth guard, did-web issuer/controller/assertionMethod +
      URL path-traversal, kid↔thumbprint binding (jws+cose), keys full strip, vc temporal fail-closed +
      proof-purpose, sd uniqueness, scitt leaf-binding, mmr overflow, signed-export guards, keyattest
      maxAge, cose dup-keys, multikey error type. Rejected false-positive class: "no trust anchor /
      embedded key" (deliberate self-contained-offline design).
- [~] server (54) — wave 1 done (18 files): auth/apikeys/crypto/hash/salt/ingest/ratelimit/bots/gpc/ai/
      csv/request-meta/collect/event/crud/admin. Fixes committed c9db131 (auth fail-closed, request-meta,
      collect key). Server wave 2 (routes stats/funnels/experiments/goals/transparency/wellknown/scitt/
      registry already covered by earlier server-integration review; remaining lib: sessions/rollups/
      retention/channel/dpv/webhook/ae/scheduled/log/security-txt/constants + db/ + app/index/env) TODO.
      NEEDS-USER-DECISION items (not code-fixable safely): see below.
- [ ] cli (13)
- [ ] cli (13)
- [ ] shared (7)
- [ ] client (4)
- [ ] dashboard (53)

## Trust batch 2 — remaining REAL candidates (verify + fix)
- sd.ts: dedup _sd digests + reject duplicate disclosed names + reject mandatory/selective name collision (SD-JWT uniqueness; real substitution surface). Regression-safe.
- scitt.ts: bind inclusion.leaf === leafHash(fromHex(statementHash)). NOTE: leaf is sha256(fromHex(statementHash)), NOT fromHex(statementHash) — must compute leafHash wrapping or it regresses. Verify with transparency test.
- mmr.ts: replace `1<<k` + clz32 with 2**Math.floor(log2()) in peakIndices/mostSigBit/indexHeight (removes 2^31 ceiling; no-op in realistic range). Safe.
- signed-export.ts: null/undefined env guard before destructure; toPublicJwkFields on embedded key. Safe.
- cose.ts: enforce tag-18 (message[0]===0xd2) + rejectDuplicateMapKeys in DECODE_OPTS (conformance). Check no external untagged COSE in tests first.
- did-web.ts: didWebToUrl reject %2f/%2e path-injection; normalize origin via new URL().origin. Careful (network path).
- LOWER/opt-in: keyattest maxAge, vc proof-purpose enforcement, multikey DOMException→Error.

## Trust wave — candidate findings triage (verify each vs source before fixing)

### REAL + regression-safe → fix
- canonicalize.ts: unbounded recursion (measured stack overflow ~2500 deep). Fix: depth guard (throw >256). Safe; legit docs are shallow.
- jws.ts verifyDetachedProof: `kid` checked for internal consistency but not bound to thumbprint(publicJwk). Signing ALWAYS sets kid=thumbprint (keys.ts:43, all sign paths use key.kid) → enforcing kid===thumbprint on verify is regression-safe + matches documented contract. Check statement.ts COSE path for same.

### REAL but needs issuance-side regression check first
- did-web.ts verifyDidConfiguration: doesn't bind credential.issuer===did or vm.controller===did (DIF spec requires). Must confirm issueDomainLinkageCredential/buildDidDocument set these before enforcing. Also: didWebToUrl decodeURIComponent path-traversal (MEDIUM), origin not normalized (MEDIUM, fail-safe dir).

### LIKELY FALSE POSITIVE / deliberate design → do NOT rip up
- checkpoint.ts "no trust anchor, verifies embedded key": DELIBERATE self-contained-offline model (caller pins key out-of-band). Confirmed in earlier verified review + documented in the function. No production caller. REJECT as vuln.
- keyattest.ts freshness one-sided (future-dating only, no maxAge): defense-in-depth only; opt-in maxAge would mirror vc/http-sig pattern. LOW priority; apply only the opt-in option, do NOT force rats callers (regression).

### CLEAN (no real findings)
base58, bytes, credentials, multikey (1 LOW error-type nit), rats, index, http-sig (hardened).

## SERVER wave-1 NEEDS-USER-DECISION (real findings, not silently fixable)
1. RATE LIMITER not bound in wrangler.jsonc → public /api/collect is UNTHROTTLED in prod (middleware
   fails open when RATE_LIMITER absent). Real CRITICAL. Fix = add unsafe.bindings ratelimit (namespace_id
   + limit/period are the user's policy) + strip `unsafe` in gen-test-wrangler.mjs like `ai`. Offered.
2. Public collect writes events for ANY/nonexistent site_id (no sites FK / existence check). Real, but
   fix adds a hot-path DB read (needs caching) + changes ingest semantics. Design decision.
3. Visitor-hash salt is co-located with hashes in D1, 90-day retention → DB leak reverses hashes→IP.
   Fix = optional HASH_PEPPER (Worker secret, mix into hash) + shorten salt retention. Secret+policy.
4. event.ts body.ip is unvalidated (feeds hash); validate IP format in ServerEventSchema. (safe, small)
5. ingest insertEvent+upsertSession non-atomic; db.batch would fix but needs queries.ts refactor (risk).

## Files audited clean (no real findings)
trust: base58, bytes, credentials, multikey, rats, trust/index, http-sig
server: crypto, apikeys, bots, gpc, ai, csv, ingest-core, crud, admin (all effectively clean)
