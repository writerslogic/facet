{
	"name": "@facet/trust",
	"version": "0.0.0",
	"private": true,
	"description": "Workers-compatible trust & provenance primitives for Facet (keys/JWKS, JWS, HTTP Message Signatures, VC, MMR).",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"lint": "biome check .",
		"format": "biome format --write .",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --passWithNoTests"
	},
	"dependencies": {
		"jose": "5.9.6"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.6.4",
		"@cloudflare/workers-types": "4.20250712.0",
		"typescript": "5.7.3",
		"vitest": "2.1.8"
	}
}
