// `countless init`: scaffold a wrangler.jsonc + .dev.vars and create the D1 database.
// Real implementation lands in T028.

export async function runInit(_args: string[]): Promise<number> {
	process.stdout.write('countless init: not yet implemented\n');
	return 0;
}
